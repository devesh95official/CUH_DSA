//iostream is included to use standard input/output streams
#include <iostream>

// Include the standard namespace to avoid prefixing std:: before standard functions
using namespace std;

// Main function of the program
int main() {

    // This is a simple C++ program that prints "Hello, World!" to the console.
    cout << "Hello, World!" << endl;

    // Return 0 to indicate successful execution
    return 0;
}